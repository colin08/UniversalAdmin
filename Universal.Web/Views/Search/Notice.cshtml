
@{
    ViewBag.Title = "搜索结果-公告";
    ViewData["tabTop"] = "null";
}

<!--头部脚本-->
@section head
{
}
<!--头部脚本 end-->

<div class="search-head mb_20">
    <div class="search-head-bd">
        <div class="search-page">
            <input type="text" id="txt_search" value="@ViewData["word"]" /><a href="javascript:;" class="btn-search" onclick="pageData(1)">搜索</a>
        </div>
        <div class="clearfix">
            <ul class="new-tab clearfix">
                <li><a href="javascript:;" onclick="tab_click('Project')">项目</a></li>
                <li><a href="javascript:;" onclick="tab_click('Document')">秘籍</a></li>
                <li><a href="javascript:;" onclick="tab_click('Node')">节点</a></li>
                <li class="current"><a href="javascript:;" onclick="tab_click('Notice')">公告</a></li>
                <li><a href="javascript:;">同事</a></li>
            </ul>
            <span class="fr f14">搜索 和 <strong class="red" id="show_search_word"></strong> 相关的节点，共<strong class="red" id="show_search_total">0</strong>个结果</span>
        </div>
    </div>
</div>
<div class="search-con clearfix">

    <table border="0" cellspacing="0" cellpadding="0" class="table1 search-border">
       <thead>
           <tr>
               <th width="20%">通知标题</th>
               <th width="30%">通知内容</th>
               <th width="10%">通知范围</th>
               <th width="10%">发布人</th>
               <th width="15%">添加时间</th>
               <th width="10%">操作</th>
           </tr>
       </thead>
        <tbody id="listData"></tbody>
    </table>

    <div class="page mt_40 tr">
        <div class="listBtm">
            <div class="page fr">
                <span>共有<span id="span_data_total"></span>条，每页显示</span>
                <select class="sel" id="select_page_size">
                    <option value="10" selected>10</option>
                    <option value="20">20</option>
                </select>
                <span>条</span>
                <div id="pager"></div>
            </div>
        </div>
    </div>

</div>

<!--wrapper ]]-->


<div class="footer">Copyright ©  深圳花伴里实业集团有限公司  粤ICP备13089427号-1</div>


<!--底部脚本-->
@section foot
{
@Scripts.Render("~/bundles/laypage")
    <script type="text/javascript">
        $(document).ready(function (e) {
            pageData(1);
        });

        /*******************列表 end *************************/
        //分页方法
        function pageData(page_index) {
            $.ajax({
                type: "post",
                url: "/Notice/NoticeData",
                data: { "page_size": $("#select_page_size").val(), "page_index": page_index, "keyword": $("#txt_search").val() },
                async: false, //同步请求
                datatype: "json",
                success: function (data) {
                    if (data.msg == 0) {
                        alert(data.msgbox);
                        return;
                    }

                    $("#show_search_word").text("\"" + $("#txt_search").val() + "\"");
                    $("#listData").html("");
                    var dataHtml = "";
                    $.each(data.data, function (i, item) {
                        dataHtml += "<tr>";
                        dataHtml += "<td><a href=\"/Notice/Modify?id=" + item.ID + "\">" + CutString(item.Title, 15) + "</a></td>";
                        dataHtml += "<td><span class=\"tfe\">" + CutString(item.Content, 48) + "</span></td>";
                        dataHtml += "<td>" + item.NoticeStr + "</td>";
                        dataHtml += "<td>" + item.CusUser.NickName + "</td>";
                        dataHtml += "<td>" + ChangeDateFormat(item.AddTime, "yyyy-MM-dd HH:mm:ss") + "</td>";
                        dataHtml += "<td><div class=\"tbOpare\"><a href=\"javascript:;\" class=\"alook\"><i class=\"i-look\"></i></a></div></td>";
                        dataHtml += "</tr>";
                    });
                    if (data.data.length == 0) {
                        dataHtml += "<tr><td colspan=\"10\"><p style='text-align:center;font-size:16px;'>没有相关数据</p></td><tr>";
                    }
                    $("#span_data_total").text(data.total);
                    $("#show_search_total").text(data.total);
                    $("#listData").html(dataHtml);
                    //$("#pager").show();
                    //$("#pager").pager({ pagenumber: page_index, pagecount: data.data.PageTotal, totalcount: data.data.DataTotal, buttonClickCallback: PageClick });
                    //显示分页
                    laypage({
                        cont: 'pager', //容器。值支持id名、原生dom对象，jquery对象。【如该容器为】：<div id="page1"></div>
                        pages: data.msgbox, //通过后台拿到的总页数
                        curr: page_index || 1, //当前页
                        groups: 7, //连续显示分页数
                        skip: true, //是否开启跳页
                        jump: function (obj, first) { //触发分页后的回调
                            if (!first) { //点击跳页触发函数自身，并传递当前页：obj.curr
                                pageData(obj.curr, doc_id, clearbox);
                            }
                        }
                    });

                },
                error: function () {
                    alert("加载失败");
                }
            })
        }
        /*******************列表 end *************************/

        //截取字符串
        function CutString(str, len) {
            if (str.length > len) {
                return str.substring(0, len) + "...";
            } else {
                return str;
            }
        }

        //格式化时间
        function ChangeDateFormat(cellval, format) {
            try {
                var date = new Date(parseInt(cellval.replace("/Date(", "").replace(")/", ""), 10));
                var month = date.getMonth() + 1 < 10 ? "0" + (date.getMonth() + 1) : date.getMonth() + 1;
                var currentDate = date.getDate() < 10 ? "0" + date.getDate() : date.getDate();
                var hour = date.getHours() < 10 ? "0" + date.getHours() : date.getHours();
                var Minute = date.getMinutes() < 10 ? "0" + date.getMinutes() : date.getMinutes();
                var Second = date.getSeconds() < 10 ? "0" + date.getSeconds() : date.getSeconds();
                switch (format) {
                    case "yyyy-MM-dd":
                        return date.getFullYear() + "-" + month + "-" + currentDate;
                    case "yyyy-MM-dd HH:mm:ss":
                        return date.getFullYear() + "-" + month + "-" + currentDate + " " + hour + ":" + Minute + ":" + Second;
                    default:
                        return date.getFullYear() + "-" + month + "-" + currentDate + " " + hour + ":" + Minute + ":" + Second;
                }
            }
            catch (e) {
                return "";
            }
        }
    </script>

    <script type="text/javascript">
        function tab_click(action) {
            var search_word = $("#txt_search").val();
            if (search_word.length == 0) {
                alert("请输入搜索内容");
                return;
            }
            location.href = "/Search/" + action + "?word=" + search_word;
        }
    </script>

}
<!--底部脚本 end-->